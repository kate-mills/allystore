import * as React from "react"

const Header = () => (
  <header style={{ background: `var(--clr-primary-11)`, marginBottom: `1.45rem`, }}>
    <div style={{ margin: `0 auto`,maxWidth: 960,padding: `1.45rem 1.0875rem`, }}>
      <h1>Skincare Webstore</h1>
    </div>
  </header>
)
export default Header
